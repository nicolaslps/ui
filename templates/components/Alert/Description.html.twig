{%- props as = 'div' -%}
<{{ as }}
    data-slot="alert-description"
    class="{{ hui('alert-description').apply({}, attributes.render('class'))|tailwind_merge }}"
    {{ attributes }}
>
    {%- block content %}{% endblock -%}
</{{ as }}>
