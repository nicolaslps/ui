{%- props as = 'div', inline = true -%}
<{{ as }}
    data-slot="alert-action"
    class="{{ hui('alert-action').apply({inline: inline}, attributes.render('class'))|tailwind_merge }}"
    {{ attributes }}
>
    {%- block content %}{% endblock -%}
</{{ as }}>
