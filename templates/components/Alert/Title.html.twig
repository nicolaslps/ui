{%- props as = 'div' -%}
<{{ as }}
    class="{{ hui('alert-title').apply({}, attributes.render('class'))|tailwind_merge }}"
    {{ attributes.defaults({
        'data-slot': 'alert-title',
    }) }}
>
    {%- block content %}{% endblock -%}
</{{ as }}>
