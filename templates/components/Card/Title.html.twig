{%- props as = 'div' -%}
<{{ as }}
    data-slot="card-title"
    class="{{ hui('card-title').apply({}, attributes.render('class'))|tailwind_merge }}"
    {{ attributes }}
>
    {%- block content %}{% endblock -%}
</{{ as }}>
