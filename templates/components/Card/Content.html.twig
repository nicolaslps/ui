{%- props as = 'div' -%}
<{{ as }}
    class="{{ hui('card-content').apply({}, attributes.render('class'))|tailwind_merge }}"
    {{ attributes.defaults({
        'data-slot': 'card-content',
    }) }}
>
    {%- block content %}{% endblock -%}
</{{ as }}>
