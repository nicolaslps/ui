{%- props as = 'div' -%}
<{{ as }}
    class="{{ hui('card-header').apply({}, attributes.render('class'))|tailwind_merge }}"
    {{ attributes.defaults({
        'data-slot': 'card-header',
    }) }}
>
    {%- block content %}{% endblock -%}
</{{ as }}>
