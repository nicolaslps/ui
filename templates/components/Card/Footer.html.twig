{%- props as = 'div' -%}
<{{ as }}
    class="{{ hui('card-footer').apply({}, attributes.render('class'))|tailwind_merge }}"
    {{ attributes.defaults({
        'data-slot': 'card-footer',
    }) }}
>
    {%- block content %}{% endblock -%}
</{{ as }}>
