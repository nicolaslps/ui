{%- props as = 'div' -%}
<{{ as }}
    data-slot="card-action"
    class="{{ hui('card-action').apply({}, attributes.render('class'))|tailwind_merge }}"
    {{ attributes }}
>
    {%- block content %}{% endblock -%}
</{{ as }}>
