{%- props as = 'div' -%}
<{{ as }}
    class="{{ hui('card-description').apply({}, attributes.render('class'))|tailwind_merge }}"
    {{ attributes.defaults({
        'data-slot': 'card-description',
    }) }}
>
    {%- block content %}{% endblock -%}
</{{ as }}>
