{%- props as = 'div', size = 'default' -%}
<{{ as }}
    class="{{ hui('card').apply({size: size}, attributes.render('class'))|tailwind_merge }}"
    {{ attributes.defaults({
        'data-slot': 'card',
        'data-size': size,
    }) }}
>
    {%- block content %}{% endblock -%}
</{{ as }}>
